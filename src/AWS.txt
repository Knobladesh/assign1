Registered a free account with Amazon Web Services.
Logged to Amazon Web Services using created account.

Created an single instance service in Sydney with Ubuntu Server 14.04 LTS (HVM)  as the amazon made image and with a t2.mirco Instance Type.

Created security group called default-web.
Added SSH (port 22) to secuiry group (default-web).
Added HTTP (port 80) to security group (default-web).
Added HTTPS (port 443) to security group (default-web).

Created/download a new key pair and securely store private key.

Select created security group default-web.

Logged onto EC2 instance using the Linux terminal using public terminal, private key and user detials.
